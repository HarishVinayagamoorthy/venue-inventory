const axios = require('axios'); async function test() { const login = await axios.post('http://localhost:3001/api/v1/auth/login', { email: 'customer@happiquick.test', password: 'Customer@123' }); const token = login.data.data.token; const bookings = await axios.get('http://localhost:3001/api/v1/bookings', { headers: { Authorization: Bearer  } }); console.log(JSON.stringify(bookings.data, null, 2)); } test();  
